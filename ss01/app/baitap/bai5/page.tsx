import React from 'react'

type props = {
  btn:string[]
}

import default function Button(props: Props){
  // console.log("props",props);
  return{
    <div>
    {props.btn.map((item,index))
    }
    </div>
  }
}

